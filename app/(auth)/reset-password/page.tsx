import { ResetPasswordRequestForm } from "./ResetPasswordRequestForm";

export const metadata = { title: "Reset password | Prime Build" };

export default function ResetPasswordPage() {
  return <ResetPasswordRequestForm />;
}
